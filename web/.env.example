# Copy to web/.env.local (gitignored, Vite loads it automatically).
# Local dev: point at your local `uvicorn` (see service/README.md).
# Not a secret -- ships in the built client bundle regardless (any
# visitor's browser calls it directly).
VITE_API_BASE_URL=http://localhost:8000
