#!/bin/sh
# Blocks commits containing secrets. Installed via: git config core.hooksPath .githooks
if ! command -v gitleaks >/dev/null 2>&1; then
    echo "gitleaks not found (brew install gitleaks) — refusing to commit without a secret scan." >&2
    exit 1
fi

gitleaks protect --staged --redact -v
